package String;

public class palindrome {
    public static void main(String[] args) {
        String s = "raceaecar";

        int i = 0,j = s.length();

        while(i<s.length()/2 && j>s.length()/2)
        {

        }
    }
}
